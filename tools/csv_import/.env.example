DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<db>
CSV_SOURCE_DIR=\\<内部サーバー名>\<共有ディスク>\<業務フォルダ>\csv
# 接続後にSELECT current_database()の結果と照合する安全確認用（DATABASE_URLの<db>と同じ値にする）
EXPECTED_DB_NAME=<db>
